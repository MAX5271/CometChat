# CometChat — AI Agent Instructions

> Auto-generated by `npx @cometchat/skills add`. Do not hand-edit; rerun the installer to refresh.
> Family: **base**. 4 skills available.

## When the user asks about CometChat

If the user asks any of the following, run the matching CLI command — DO NOT improvise from training memory:

| User says... | Run |
|---|---|
| "integrate cometchat" / "add chat" / "/cometchat" | `cometchat init` (or walk the user through `detect` → `auth login` → `provision setup` → `apply` → `verify`) |
| "what's my framework" | `cometchat detect --json` |
| "log me into CometChat" / "set up credentials" | `cometchat auth login` then `cometchat provision setup --app-id <id> --framework <fw>` |
| "design chat visually" / "open the UI Kit Builder" | `cometchat builder open --json` (returns a deep URL into the dashboard's UI Kit Builder), then `cometchat builder export --target . --json` after the user designs |
| "add polls" / "enable smart replies" / "turn on \<feature\>" | `cometchat features list` to find the feature id, then `cometchat apply-feature <id>` |
| "match my brand" / "theme it like Slack" | `cometchat apply-theme --preset slack` (or `--primary-color #...`) |
| "use real auth tokens, not the auth key" | `cometchat production-auth` |
| "what went wrong" / "diagnose this" | `cometchat doctor` |
| "what's installed already" | `cometchat info` or `cometchat status` |

Run `cometchat --help` for the full command list. Each subcommand has `--help` with detailed usage.

## How CometChat integration works (base)

1. **Detect** the framework — `cometchat detect --json` returns framework, version, env-var prefix, and existing-integration state.
2. **Authenticate** — the user logs into the CometChat dashboard via `cometchat auth login` (browser flow). Token is stored in OS keychain.
3. **Provision** — `cometchat provision setup --app-id <id> --framework <fw>` writes credentials to the right env file (`.env` for Vite, `.env.local` for Next.js, `local.properties` for Android, `Secrets.swift` for iOS, etc.).
4. **Apply** — `cometchat apply --experience <N>` writes the integration code (provider, components, placement). Idempotent.
5. **Verify** — `cometchat verify` runs build + AST checks. `cometchat doctor` runs combined diagnostics.
6. **Iterate** — features / theming / customization via the CLI commands above.

## Hard rules (non-negotiable)

- **Run `cometchat detect` BEFORE making framework assumptions.** The user's project might be React+Vite, Next.js App Router, React Router v7, Astro, Expo, bare RN, Angular 12-15, native Android (V5 stable + V6 beta), iOS V5, or Flutter (V5 stable + V6 beta) — each has different conventions.
- **NEVER hardcode credentials.** Use `cometchat provision setup`, which writes the right env file with the right prefix. The Auth Key is for dev only — `cometchat production-auth` upgrades to server-minted tokens.
- **NEVER hand-roll a custom search UI** — the UI Kit ships `<CometChatSearch>` (and `showSearchBar` on `<CometChatConversations>`). Hand-rolling silently breaks pagination + result highlighting.
- **NEVER `Skill()`-style auto-load skills.** This file is your orientation; for deeper detail, run the CLI command for the specific task. The CLI's `--help` text is the source of truth.
- **Component names + props** — for an unfamiliar component, run `cometchat features info <name>` or query the CometChat docs MCP if available. Don't invent from training data; the UI Kit's API surface has changed across versions.

## When you need richer guidance

Each `cometchat <cmd> --help` covers usage for that command. For deeper skill content (placement patterns, customization four-tier model, troubleshooting symptom-table, framework-specific gotchas) — install the multi-file skill set:

```bash
npx @cometchat/skills add --family base --ide claude
```

That writes 4 per-skill markdown files to `.claude/skills/cometchat-*/SKILL.md`. Cursor / Continue / Cline can read those too — open the files directly when needed (`@.claude/skills/cometchat-core/SKILL.md` in Cursor, etc.).
